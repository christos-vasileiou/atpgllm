# Experiments (non-packaged R&D)

- `legacy/` — older training/classification CLIs and Slurm wrappers
- `scratch/` — ad-hoc scripts, plots, templates
- `notebooks/` — Jupyter notebooks
- `digitest.llm/` — nested external checkout (has its own `.git`); not part of the `atpgllm` package
