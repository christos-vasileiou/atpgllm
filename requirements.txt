# ATPG LLM training stack - aligned for transformers 4.57.3
# Core: transformers 4.57.3 with compatible dependencies

# Transformers and its strict dependencies (from PyPI 4.57.3)
transformers==4.57.3
huggingface-hub>=0.34.0,<1.0
tokenizers>=0.22.0,<=0.23.0
safetensors>=0.4.3
accelerate>=0.26.0
torch>=2.2

# PEFT, TRL, bitsandbytes (per training_code.py)
peft==0.13.2
trl==0.26.1
bitsandbytes==0.49.0

# Supporting packages
datasets>=3.0.0
numpy>=1.17
packaging>=20.0
pyyaml>=5.1
regex!=2019.12.17
requests
tqdm>=4.27
filelock
